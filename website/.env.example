# Frontend env (safe to commit)
# Do NOT put secrets here. Only VITE_* variables are exposed to the browser.

# Points the website to the admin API base (Next.js server)
VITE_ADMIN_API_BASE=http://localhost:3000

# Optional: if you embed Google Maps or similar services
VITE_GOOGLE_MAPS_API_KEY=

# Optional: YouTube Data API integration (for Videos page)
VITE_YOUTUBE_API_KEY=
VITE_YOUTUBE_CHANNEL_ID=
